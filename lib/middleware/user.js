import controllers from 'controllers';
import Logger from 'util/Logger';

const log = new Logger('mw/user');

export default {
  getUser,
};

function getUser(req, res, next) {
  // need to implement get user
  res.json(200);
}

